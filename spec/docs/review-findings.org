#+TITLE: Deep Review Findings (2026-02-27)
#+SUBTITLE: HISTORICAL - Most items resolved. See next-steps.org for remaining work.

Items to revisit from the 6-agent deep review of spec and implementations.

* DONE: Implementation Bugs (fixed 2026-02-27/28)

** DONE Go
- CRLF: stripped =\r= from line endings
- Whitespace: now strips all leading/trailing whitespace from values
- Escape: fixed =findUnescapedSep= — simple =\= before sep check, no pair processing

** DONE Zig
- =---= handling: now treated as bare key, not header separator

** DONE Python and JavaScript
- Serializers now handle multiline values (continuation lines)
- Recursive =\== splitting fixed — first split only
- Escape: fixed =findUnescapedSeparator= — simple =\= before sep check, no pair processing

** DONE Remaining (resolved 2026-02-28)
- Go: continuation vs nesting — fixed (W001 semantics)
- Go: empty value compaction — fixed
- Python, Zig, JavaScript: recursion depth limit — enforced (100 levels)
- Zig: O(n^2) merge — fixed (OrderedMap uses hash index)
- JavaScript: O(n^2) merge — fixed (in-place mutation)

** Still open
- Emacs: no input size limit (should be 10MB), no recursion depth limit

* DONE: Fixture Issues (fixed 2026-02-27/28)

** DONE Wrong categorical outputs
- List-marker categorical fixtures reverted to arrays (correct — preserves order)

** DONE Backslash-decrement spec/fixture disagreement
- Resolved: fixture was correct, old spec example was wrong (used pair processing)
- Simple rule =\sep → sep= applied left-to-right: =x\\\=y= → =x\\=y= (two literal =\=, then =\== escape)
- New fixture =escape/double-backslash-before-sep= tests bare-key case

** DONE Cross-implementation divergences
- Inline === splitting: DECIDED — first split only (all impls fixed)
- =\\== tokenization: DECIDED — simple rule, no pair processing (all impls fixed)

* Missing Fixtures (reduced from 22 gaps)

** Added
- =:== separator (=header/colon-equals-separator=)
- Unescaped === in values (=valid/separator-in-value=)
- Left-identity merge (=merge/left-identity=)
- =\\== at split position (=escape/double-backslash-before-sep=)

** Still missing
- Empty key pattern (== value=)
- Custom separator beyond standard set
- Merge associativity verification (3-way merge)
- Header options (=space-before=false=, =compact=true=, etc.)
- Tab-based indentation
- Strict indent mode header options
- Nested list markers
- Separator-on-same-line-as-list-marker rejection
- Missing-separator error detection
- Malformed header error detection
- Deeply nested objects (5+ levels)
- Blank line handling
- Categorical union merge (overlapping repeated keys)

* Editor Modes
- Tree-sitter grammar created but not yet tested with actual editors
- VS Code extension JSON files validated but not tested in VS Code
- Vim plugin not tested in Vim
